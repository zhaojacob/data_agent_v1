# Data Agent Demo 阶段总结报告

> 日期：2026-01-13  
> 状态：Demo 版本已部署到 Render  
> 目标：金融领域的 Google Deep Research

---

## 一、项目概述

Data Agent 是一个基于 LangGraph 的智能数据分析平台，目标是成为金融领域的 Deep Research 工具，能够：
- 连接金融数据库，执行复杂查询
- 安全执行 Python 代码进行数据分析
- 生成专业级可视化图表
- 最终生成结构化研究报告

---

## 二、Demo 阶段成果

### 2.1 已完成功能

| 模块 | 状态 | 说明 |
|------|------|------|
| LangGraph Agent | ✅ | 单一 ReAct Agent，支持多工具调用 |
| SQL 查询 | ✅ | 连接 Supabase PostgreSQL，带安全验证 |
| Python 执行 | ✅ | E2B 云端沙盒，完全隔离 |
| 数据可视化 | ✅ | matplotlib/seaborn 图表生成 |
| 网络搜索 | ✅ | Tavily API 集成 |
| Web 前端 | ✅ | FastAPI + Chainlit 挂载模式 |
| 流式输出 | ✅ | 实时显示 Agent 思考过程 |
| 云端部署 | ✅ | Render 容器化部署 |

### 2.2 技术架构

```
┌─────────────────────────────────────────────────────┐
│         Render 服务 ($7/月)                          │
│  ┌───────────────────────────────────────────────┐ │
│  │  app.py (FastAPI + Chainlit)                   │ │
│  │    ├── /              聊天界面                  │ │
│  │    ├── /api/agent/invoke    同步 API           │ │
│  │    ├── /api/agent/stream    流式 API (SSE)     │ │
│  │    └── /api/trigger-report  Webhook            │ │
│  └───────────────────────────────────────────────┘ │
│                        ↓                            │
│  ┌───────────────────────────────────────────────┐ │
│  │  graph.py (LangGraph Agent)                    │ │
│  │    ├── sql_inter      → Supabase PostgreSQL    │ │
│  │    ├── python_inter   → E2B 沙盒               │ │
│  │    ├── fig_inter      → E2B 沙盒 + 图片下载    │ │
│  │    └── search_tool    → Tavily API             │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 2.3 安全机制

- **SQL 安全**：双重防护（代码层 `validate_sql()` + 数据库只读用户）
- **代码执行安全**：E2B 云端沙盒，与主机完全隔离
- **系统提示词限制**：禁止危险操作（系统命令、网络请求、文件写入等）

---

## 三、关键技术问题及解决方案

### 3.1 Chainlit 流式响应问题

**问题**：Agent 返回的 chunk key 是 `'model'` 而非预期的 `'agent'`，导致响应为空。

**解决**：修改 `stream_agent_response` 函数，动态处理所有 key 类型。

### 3.2 异步生成器问题

**问题**：`agent.stream()` 是同步的，但 Chainlit 需要异步生成器。

**解决**：使用 `asyncio.run_in_executor` 在线程池中运行同步代码。

### 3.3 E2B 沙盒偶发连接失败

**问题**：从沙盒下载图片时偶尔报 `[WinError 10054]`。

**现状**：Agent 会自动重试，通常第二次成功。后续可添加显式重试逻辑。

---

## 四、与目标的差距分析

### 4.1 当前状态 vs Deep Research

| 维度 | Deep Research | 当前 Demo | 差距 |
|------|--------------|-----------|------|
| Agent 架构 | Multi-Agent 系统 | 单一 ReAct Agent | ⭐⭐⭐⭐⭐ |
| 推理模式 | 分层规划 + 反思 | 简单 ReAct | ⭐⭐⭐⭐⭐ |
| 数据源 | 专业金融数据 | 测试数据 | ⭐⭐⭐⭐⭐ |
| 报告输出 | 结构化研报 | 对话回复 | ⭐⭐⭐⭐⭐ |
| 记忆系统 | 长期 + 工作记忆 | 会话内记忆 | ⭐⭐⭐⭐ |

### 4.2 核心差距

1. **架构差距**：单 Agent 无法处理复杂的多步骤研究任务
2. **数据差距**：缺乏真实金融数据（行情、财务、新闻、研报）
3. **输出差距**：无法生成结构化、可引用的研究报告

---

## 五、重点发力方向建议

### 5.1 短期（1-2 周）：基础设施完善

| 任务 | 优先级 | 预计时间 | 理由 |
|------|--------|----------|------|
| 图片云存储 | P0 | 2-3 小时 | Render 重启会丢失图片 |
| 会话记忆持久化 | P0 | 2-3 小时 | 刷新页面丢失对话 |
| 冷启动优化 | P1 | 30 分钟 | 首次访问太慢 |
| 接入 AKShare | P1 | 1 天 | 获取真实金融数据 |

### 5.2 中期（2-4 周）：核心能力建设

**建议重点：先做"个股分析报告"这一个垂直场景**

理由：
- 固定模板，输出可控、可验证
- 用户需求明确，容易获得反馈
- 可以逐步积累金融领域的 Context Engineering 经验

具体任务：
1. **设计个股分析报告模板**（参考券商研报格式）
2. **接入金融数据源**（AKShare + 你的 financial-news-brief）
3. **实现 Multi-Agent 架构**（Supervisor + Researcher + Analyst + Writer）
4. **添加向量数据库**（存储新闻、研报，支持语义检索）

### 5.3 长期（1-2 月）：产品化

- 用户认证与权限
- 使用额度管理
- 监控与成本追踪
- 考虑 Next.js 前端重构（如需复杂 UI）

---

## 六、Context Engineering 策略建议

这是决定产品质量的关键，建议采用以下策略：

### 6.1 工具返回结构化摘要

```python
# 不要返回原始数据
def search_news(keyword):
    return {
        "total": 100,
        "top_5": [...],  # 只返回最相关的
        "sentiment": {"positive": 60, "negative": 30},
        "hint": "如需详情，调用 get_news_detail(id)"
    }
```

### 6.2 分层上下文

```
System Prompt（固定）
    ↓
任务上下文（动态注入：研究主题、已完成步骤）
    ↓
工作记忆（最近对话、当前工具结果）
```

### 6.3 研究模板库

为不同任务类型预设 Prompt 模板：
- 个股分析模板
- 行业对比模板
- 事件影响分析模板

---

## 七、项目文档索引

| 文档 | 说明 |
|------|------|
| `PROJECT_ROADMAP.md` | 项目总路线图 |
| `FRONTEND_ROADMAP.md` | 前端开发路线图 |
| `BACKEND_ROADMAP.md` | 后端开发路线图 |
| `DEPLOYMENT_LOG.md` | 部署日志 |
| `SECURITY_GUIDE.md` | 安全加固指南 |

---

## 八、结论

Demo 阶段已成功验证核心技术可行性：
- LangGraph Agent 能够协调多工具完成数据分析任务
- E2B 沙盒提供了安全的代码执行环境
- FastAPI + Chainlit 架构支持流式输出和 API 扩展

**下一步关键**：
1. 完善基础设施（图片存储、会话持久化）
2. 接入真实金融数据
3. 聚焦"个股分析报告"场景，做深做透
4. 逐步升级为 Multi-Agent 架构

项目已具备从 Demo 走向 MVP 的基础，建议按照 `PROJECT_ROADMAP.md` 中的阶段规划推进。

---

**报告撰写**：Kiro AI Assistant  
**日期**：2026-01-13
